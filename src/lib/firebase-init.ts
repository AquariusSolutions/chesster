/**
 * Native: the default Firebase app is created automatically from
 * google-services.json / GoogleService-Info.plist, so there is nothing to do.
 * The web counterpart lives in firebase-init.web.ts.
 */
export async function initFirebase(): Promise<void> {}
